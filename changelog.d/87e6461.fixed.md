- Add `MAX_PATTERN_BYTES` guards to `/info`, `/history`, `/options` daemon handlers
- Apply `limit` parameter correctly in `/options` handler
- Fix TUI character loss when typing quickly by accumulating `pending_query`
- Fix TUI cache key to include search mode, field, and flags
- Fix silent `SizeAsc`/`SizeDesc` mapping in TUI package search
- Fix ngram cache self-deadlock in `resolve_ngram_ordinals_multi`
