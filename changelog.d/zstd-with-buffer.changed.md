- Open `zstd::stream::read::Decoder` with `with_buffer` / `single_frame` for `&[u8]` and `Cursor<&[u8]>` inputs, avoiding an extra `BufReader` layer and an unnecessary multi-frame scan during database locate operations.
